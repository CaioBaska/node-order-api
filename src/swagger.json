{
  "openapi": "3.0.0",
  "info": {
    "title": "Order API",
    "version": "1.0.0"
  },
  "paths": {
    "/order": {
      "post": {
        "summary": "Criar pedido",
        "requestBody": { "content": { "application/json": { "schema": { "type": "object" } } } },
        "responses": { "201": { "description": "Criado" } }
      }
    },
    "/order/{orderId}": {
      "get": { "summary": "Obter pedido", "parameters": [{ "name": "orderId", "in": "path", "required": true }], "responses": { "200": { "description": "OK" } } },
      "put": { "summary": "Atualizar pedido", "parameters": [{ "name": "orderId", "in": "path", "required": true }], "requestBody": { "content": { "application/json": { "schema": { "type": "object" } } } }, "responses": { "200": { "description": "Atualizado" } } },
      "delete": { "summary": "Deletar pedido", "parameters": [{ "name": "orderId", "in": "path", "required": true }], "responses": { "204": { "description": "Deletado" } } }
    },
    "/order/list": {
      "get": { "summary": "Listar pedidos", "responses": { "200": { "description": "OK" } } }
    }
  }
}